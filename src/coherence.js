require('core-js');

const Coherence = {};

Coherence.NAVIGATE_ACTION_TYPE = require('./navigate_action_type');
Coherence.Controller = require('./controller');
Coherence.Model = require('./model');
Coherence.Bindings = require('./bindings');

module.exports = Coherence;
